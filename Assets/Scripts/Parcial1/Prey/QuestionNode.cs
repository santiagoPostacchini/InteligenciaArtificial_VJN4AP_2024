using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuestionNode //: P_Node
{
    //public P_Node trueNode;
    //public P_Node falseNode;

    //public TypeQuestion type;

    //public override void Execute(Boid boid)
    //{
    //    switch (type)
    //    {
    //        case TypeQuestion.FoodNear:
    //            if (boid.CheckFoodNear(GameManager.Instance.foods, GameManager.Instance.radiusDetect))
    //                trueNode.Execute(boid);
    //            else
    //                falseNode.Execute(boid);
    //            break;

    //        case TypeQuestion.FlockNear:
    //            if(boid.CheckFlockNear(GameManager.Instance.boids, GameManager.Instance.radiusDetect))
    //                trueNode.Execute (boid); 
    //            else
    //                falseNode.Execute(boid);
    //            break;

    //        case TypeQuestion.HunterNear:
    //            if(boid.CheckHunterNear(GameManager.Instance.hunter, GameManager.Instance.radiusDetect))
    //                trueNode.Execute(boid);
    //            else
    //                falseNode.Execute(boid);
    //            break;
    //    }
    //}

    //public enum TypeQuestion
    //{
    //    FoodNear,
    //    FlockNear,
    //    HunterNear
    //}
}
